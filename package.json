{
  "name": "theylive-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:reports && vite build",
    "build:reports": "node scripts/build-reports.mjs",
    "preview": "vite preview",
    "deploy": "wrangler deploy --env production"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
